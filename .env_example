DISCORD_TOKEN=your_discord_bot_token
BOT_OWNER_ID=your_discord_user_id

ENABLE_PRODUCTION_MODE=false

# Cron configuration for build ID refresh
# Set to "true" or "1" to enable, "false" or "0" to disable
CRON_ENABLED=true

# Cron schedule for build ID refresh (cron format with seconds)
# Default: "0 0 */6 * * *" (every 6 hours)
# Examples:
#   "0 0 */6 * * *"  - Every 6 hours
#   "0 0 */2 * * *"  - Every 2 hours  
#   "0 0 * * * *"    - Every hour
#   "0 */30 * * * *" - Every 30 minutes
CRON_SCHEDULE=0 0 */6 * * *

# Audio Pipeline Configuration (optional - defaults to config/audio.yaml)
# AUDIO_RETRY_COUNT=3
# AUDIO_TIMEOUT=30
# AUDIO_LOG_LEVEL=info
# AUDIO_FFMPEG_BINARY=ffmpeg
# AUDIO_FFMPEG_FORMAT=s16le
# AUDIO_FFMPEG_SAMPLE_RATE=48000
# AUDIO_FFMPEG_CHANNELS=2
# AUDIO_FFMPEG_OPTIONS="-reconnect,1,-reconnect_delay_max,5"
# AUDIO_FFMPEG_CUSTOM_ARGS="-reconnect,1,-reconnect_delay_max,5"
# AUDIO_OPUS_BITRATE=128000
# AUDIO_OPUS_FRAME_SIZE=960
# AUDIO_MAX_RETRIES=3
# AUDIO_BASE_DELAY=2s
# AUDIO_MAX_DELAY=30s
# AUDIO_RETRY_MULTIPLIER=2.0
# AUDIO_LOG_FORMAT=json
# AUDIO_LOG_SAVE_DB=true

